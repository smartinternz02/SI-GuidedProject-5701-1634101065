from setuptools import setup, find_packages
from codecs import open
from os import path
here=path.abspath(path.dirname(__file__))
setup(
      name='Python FLask App weather',
      version=1.0.0,
      description='Hello World app for running python apps on bluemix',
      license='Apache-2.0'
      )